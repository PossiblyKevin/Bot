// Optional: You can hardcode your keys directly here if you want them pre-baked 
// into your private repository clone, otherwise use the UI inputs.
export const DEFAULT_CONFIG = {
  apiKey: "",
  apiSecret: "",
  baseUrl: "https://api.toobit.com" // Direct Toobit REST base URL
};